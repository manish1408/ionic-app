import { ButtonSpinnerDirective } from './button-spinner.directive';

describe('ButtonSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSpinnerDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
