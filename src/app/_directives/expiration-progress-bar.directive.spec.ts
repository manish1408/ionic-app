import { ExpirationProgressBarDirective } from './expiration-progress-bar.directive';

describe('ExpirationProgressBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpirationProgressBarDirective(undefined, undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
